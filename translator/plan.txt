General overview.

User:
    - can register with google auth
    - can login
    - can logout
User:
    - types in a word in foreign language (Armenian)
        > typing is done through pop-up keyboard with language symbols
System: 
    if the word exists in the dictionary:
        + form with translation is displayed
        # form with suggested pictures is displayed
            image source: https://pixabay.com/api/docs/
            > if picture is chosen for the card - download picture and store them on an owned server 
        # button "Save as a card" is displayed
    otherwise:
        + list of similar existing words is displayed
        + words in the list are clickable
        +- if clicked, the word is put into main input and search is executed
Authenticated User: 
    - saves a card
    - can see the list of saved cards
    - can open each card

Progress.

completed:
# input handling through typing and keyboard pop-up
# input language detection
# usage of tranlsation API (free version)
# Armenian dictionary loading
# validating a word through the dictionary
# suggested words generation through patterns (similar letters + dictionary validating)
# suggested words auto-translation through clicking

issues:
- possible to enter several words, not handled
- manually input word is not checked with dictionary before sending to API
- search cursor is not handled, every input goes to the line ending
- no checks for invalid input
- no UI error handling 

refactor: 
- use Promise.all for search action

AI tips: 

##################################################################################
Refactor for Images:

Sign up for Unsplash.
Create imageService.ts.
Write a fetchImages(query: string) function.
TS Challenge: The Unsplash response is huge. Practice using interface to model only the parts you need (e.g., urls.small, alt_description).

##################################################################################